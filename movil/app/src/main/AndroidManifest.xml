<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="CashScan"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Reconocimientobilletes"
        tools:targetApi="31">
        <activity
            android:name=".ScanBillActivity"
            android:exported="false"
            android:label="@string/title_activity_scan_bill"
            android:theme="@style/Theme.Reconocimientobilletes" />
        <activity
            android:name=".CountBillActivity"
            android:exported="false"
            android:label="@string/title_activity_count_bill"
            android:theme="@style/Theme.Reconocimientobilletes" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Reconocimientobilletes">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="reconocimiento-de-billetes.vercel.app"
                    android:scheme="https" />
            </intent-filter>

            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="reconocimiento-de-billetes.vercel.app"
                    android:scheme="http" />
            </intent-filter>
        </activity>
    </application>

</manifest>